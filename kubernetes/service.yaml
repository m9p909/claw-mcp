apiVersion: v1
kind: Service
metadata:
  name: claw-service
  namespace: default
spec:
  selector:
    app: claw
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
